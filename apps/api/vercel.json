{
  "version": 2,
  "buildCommand": "corepack enable && corepack prepare pnpm@8.12.0 --activate && corepack pnpm install --no-frozen-lockfile && corepack pnpm --filter @edustats/shared build && corepack pnpm --filter @edustats/api build",
  "outputDirectory": ".",
  "installCommand": "corepack enable && corepack prepare pnpm@8.12.0 --activate && corepack pnpm install --no-frozen-lockfile",
  "framework": null,
  "functions": {
    "api/server.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/server"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "regions": ["cdg1"]
}

