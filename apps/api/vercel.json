{
  "version": 2,
  "buildCommand": "cd ../.. && npm install -g pnpm@8.12.0 && pnpm install && cd apps/api && pnpm build",
  "outputDirectory": "dist",
  "devCommand": "cd ../.. && npm install -g pnpm@8.12.0 && pnpm install && cd apps/api && pnpm dev",
  "installCommand": "cd ../.. && npm install -g pnpm@8.12.0 && pnpm install",
  "framework": null,
  "functions": {
    "api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/server"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "regions": ["cdg1"]
}

