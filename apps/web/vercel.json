{
  "version": 2,
  "buildCommand": "corepack enable && corepack prepare pnpm@8.12.0 --activate && pnpm install --frozen-lockfile && pnpm --filter @edustats/shared build && pnpm --filter @scolarflow/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "corepack enable && corepack prepare pnpm@8.12.0 --activate && pnpm install --frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}

